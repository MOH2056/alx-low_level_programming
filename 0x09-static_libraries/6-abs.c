#include "main.h"

/**
 * _abs - cthis function is to computes the absolute value of an integer
 * @n: parameter
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
